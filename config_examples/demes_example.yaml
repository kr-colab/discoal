# Simulate using an external demes file to specify the demographic model.
# Equivalent to: TODO CLI

simulation:
  sample_size: 15
  num_replicates: 10
  num_sites: 50000

genetics:
  mutation_rate: 20.0
  recombination_rate: 15.0

demography:
  deme_sample_size: [10, 5]
  demes_filename: "../testing/two_pop_migration.yaml"

